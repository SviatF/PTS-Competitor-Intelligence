name: Legacy Meta Keyword Scanner (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy keyword scanner is disabled. Use exact-production.yml."
