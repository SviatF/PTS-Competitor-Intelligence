name: Exact Production Competitor Scanner (Disabled Duplicate)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use exact-facebook.yml production workflow."
